# N.B. If you update .gitignore, make sure to update the `git_clean` rule in
# Makefile.

# Garbage.
.DS_Store
__pycache__
coptic.egg-info

# Flashcard decks are too big to host on GitHub.
flashcards/data/*.apkg

# This contains secrets (authorization tokens for Drive, and local file paths).
secrets.sh
